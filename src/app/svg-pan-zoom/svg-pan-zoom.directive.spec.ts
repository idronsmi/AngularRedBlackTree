import { SvgPanZoomDirective } from './svg-pan-zoom.directive';

describe('SvgPanZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgPanZoomDirective();
    expect(directive).toBeTruthy();
  });
});
